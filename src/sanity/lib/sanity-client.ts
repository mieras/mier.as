// This file is deprecated - use sanity:client from @sanity/astro instead
// Keeping for backwards compatibility with image.ts
import { sanityClient } from 'sanity:client';

// Re-export as client for backwards compatibility
export const client = sanityClient;
